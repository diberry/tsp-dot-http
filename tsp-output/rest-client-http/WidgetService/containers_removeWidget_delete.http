### removeWidget

// Variables section - defines variables to use in the request
@host = localhost
@port = 8080
@baseUrl = http://{{host}}{{#if port}}:{{port}}{{/if}}

// Path parameters
@id = REPLACE_WITH_ID_VALUE
@widgetId = REPLACE_WITH_WIDGETID_VALUE

DELETE {{baseUrl}}/containers/{{id}}/widgets/{{widgetId}}

// No body allowed for this request method